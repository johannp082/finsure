[pytest]
testpaths = tests
python_files = test_*.py
addopts = -v
# Put the app/ folder (rootdir) on sys.path so `import app` works regardless of
# whether pytest is run as `pytest` or `python -m pytest` (e.g. in CI).
pythonpath = .
